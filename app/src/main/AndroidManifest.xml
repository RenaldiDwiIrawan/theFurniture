<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.thefurniture">

    <uses-feature android:name="android.hardware.camera.ar" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheFurniture"
        tools:targetApi="31">
        <activity
            android:name=".ActivityARMeja1"
            android:exported="false" />
        <activity
            android:name=".ActivityARLemari1"
            android:exported="false" />
        <activity android:name=".ActivityMeja" />
        <activity android:name=".ActivityLemari" />
        <activity android:name=".ActivitySofa" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ActivityARSofa1" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>